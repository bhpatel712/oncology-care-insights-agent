"""
Embedder — generates embeddings for guideline chunks using text-embedding-3-large.
"""

# TODO Week 2: Implement embedding logic

def embed_chunks(chunks: list) -> list:
    """
    Takes a list of chunk dicts and adds an 'embedding' field to each.
    Returns the enriched list.
    """
    pass
